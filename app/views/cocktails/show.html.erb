<%= render 'layouts/shared/navbar' %>

<div class="container-show">
  <div class="card-cocktail-show">
    <h1><%= @cocktail.name %></h1>
    <div class="ingredients">
      <% if @cocktail.doses.blank? %>
        <p>No ingredients added yet!</p>
        <p>You should change that...</p>
      <% else %>
        <p>Ingredients</p>
      <% end %>
        <% @cocktail.doses.each do |d| %>
            <li><%= d.ingredient.name %></li>
            <li><%= d.description %></li>
            <p><%= link_to "🗑", dose_path(d), method: :delete %></p>
        <% end %>
        </div>
      <div class="links">
            <p><%= link_to "Add a dosage", new_cocktail_dose_path(@cocktail[:id]) %>/<%= link_to "Cocktail List", cocktails_path %></p>
      </div>
  </div>
</div>

